# ============================================================================
# Restore Marketing Co — Automation Config
# ============================================================================
# Fill this in to turn the orchestration from scaffolding into a live system.
# Every field with a TODO marker needs your input. Every field without a TODO
# has a reasonable default. Comments explain what the field drives.
#
# This file is read by every agent at runtime. When you change it, agents
# pick up the change on their next run — no restart needed.
#
# NEVER put API keys or secrets in this file. Credentials go in .env (see
# integrations.md for the pattern).
# ============================================================================

company:
  name: "Restore Marketing Co"
  legal_entity: "TODO: LLC name / DBA / corporation"
  owner: "TODO: your full legal name (shows on contracts)"
  email: "TODO: primary business email"
  phone: "TODO: business phone (optional but useful for contracts)"
  website: "TODO: https://restoremarketing.co or similar"
  time_zone: "TODO: e.g. America/New_York, America/Los_Angeles"
  business_hours:
    start: "09:00"
    end: "17:00"
    days: ["Mon", "Tue", "Wed", "Thu", "Fri"]
  tax_id: "TODO: EIN / Tax ID (only used on invoices)"
  address:
    line1: "TODO"
    line2: ""
    city: "TODO"
    state: "TODO"
    zip: "TODO"
    country: "USA"

# ============================================================================
# SERVICES — what Restore Marketing Co actually sells
# ============================================================================
# Every proposal the automation generates is built from one or more of these.
# List the services you ACTUALLY deliver. If you have more, add them.
# ============================================================================

services:
  # -----------------------------------------------------------------------
  # WEBSITES — project work, fixed fee
  # -----------------------------------------------------------------------
  - id: "website-build"
    name: "Website Design & Build"
    short_description: "Brand-aligned, conversion-focused website designed, built, and launched — mobile-first, accessible, fast, and search-engine ready from day one."
    deliverables:
      - "Discovery and brand audit"
      - "Sitemap and page-by-page content plan"
      - "Visual design (desktop + mobile)"
      - "Built site on chosen CMS (WordPress / Webflow / Shopify — TODO: confirm default)"
      - "Core Web Vitals optimization (LCP, CLS, INP targets met)"
      - "On-page SEO baseline (meta, schema, internal linking)"
      - "Google Analytics 4 + conversion tracking setup"
      - "Launch checklist + 14-day post-launch support"
    pricing:
      model: "fixed"
      amount: 0                 # TODO: base website project price — see Q4 ask
      currency: "USD"
    typical_timeline_days: 45
    ideal_for: "Local service businesses launching new, relaunching, or rebuilding their site to drive more leads"

  # -----------------------------------------------------------------------
  # FULL-SERVICE RETAINER — the primary monthly offer
  # -----------------------------------------------------------------------
  - id: "full-service-retainer"
    name: "Full-Service Marketing Retainer"
    short_description: "Monthly retainer combining paid ads management, SEO, Google Business Profile, content, and reporting. One partner, one bill, one accountable team."
    deliverables:
      - "Paid ads management (Google Ads, Meta Ads — platforms per engagement)"
      - "SEO build-out and ongoing optimization (technical, on-page, content)"
      - "Google Business Profile management (posts, photos, reviews, Q&A, local pack work)"
      - "Content production aligned to keyword strategy"
      - "Weekly progress update + monthly performance report"
      - "Quarterly strategy review + roadmap adjustment"
    pricing:
      model: "retainer"
      amount: 0                 # TODO: monthly retainer floor — see Q4 ask
      currency: "USD"
      billing_frequency: "monthly"
    typical_timeline_days: 30
    ideal_for: "Local service businesses doing $500k–$10M annual revenue, ready to commit to 3+ months of compounding marketing investment"

  # -----------------------------------------------------------------------
  # PAID ADS MANAGEMENT — standalone retainer
  # -----------------------------------------------------------------------
  - id: "ads-management"
    name: "Paid Ads Management"
    short_description: "Google Ads and Meta Ads managed monthly — creative, targeting, bid strategy, conversion tracking, and optimization."
    deliverables:
      - "Account audit + restructure (month 1)"
      - "Campaign creation / migration"
      - "Ad creative (copy + images) — 3-5 variants per campaign"
      - "Weekly bid + budget + targeting optimization"
      - "Conversion tracking setup + reporting"
      - "Monthly performance report with recommendations"
    pricing:
      model: "retainer"
      amount: 0                 # TODO: monthly ads management fee — see Q4 ask
      currency: "USD"
      billing_frequency: "monthly"
      notes: "Ad spend billed separately / passed through to client — management fee is for the management work only"
    typical_timeline_days: 30
    ideal_for: "Businesses with $2k+/month ad budget looking for hands-off management"

  # -----------------------------------------------------------------------
  # SEO SERVICES — standalone retainer
  # -----------------------------------------------------------------------
  - id: "seo-services"
    name: "SEO Build-out & Optimization"
    short_description: "Monthly SEO work: technical, on-page, content, local, and authority building. Track rankings, traffic, and leads."
    deliverables:
      - "Technical SEO audit + fixes (ongoing)"
      - "On-page optimization (titles, meta, schema, internal links)"
      - "Keyword strategy + content briefs"
      - "Content production (1-4 pieces/month depending on tier)"
      - "Local SEO (NAP consistency, citations, local schema)"
      - "Rankings + traffic + lead monitoring"
      - "Monthly performance report"
    pricing:
      model: "retainer"
      amount: 0                 # TODO: monthly SEO retainer — see Q4 ask
      currency: "USD"
      billing_frequency: "monthly"
    typical_timeline_days: 30
    ideal_for: "Businesses that want to compound organic traffic over 6-12+ months"

  # -----------------------------------------------------------------------
  # GOOGLE BUSINESS PROFILE MANAGEMENT — standalone or bolt-on
  # -----------------------------------------------------------------------
  - id: "gbp-management"
    name: "Google Business Profile Management"
    short_description: "GBP optimization, weekly posts, photo updates, review response, Q&A, and local pack ranking work."
    deliverables:
      - "GBP audit + optimization"
      - "Weekly posts (4-5/month)"
      - "Photo additions monthly"
      - "Review response within 24 hours"
      - "Q&A management"
      - "Local pack ranking tracking + monthly report"
    pricing:
      model: "retainer"
      amount: 0                 # TODO: monthly GBP management fee — see Q4 ask
      currency: "USD"
      billing_frequency: "monthly"
    typical_timeline_days: 30
    ideal_for: "Local service businesses where customers search 'near me' / 'in [city]' — often bundled with SEO or full-service retainer"

  # Add more services as needed — the automation handles any number

# ============================================================================
# PRICING FLOOR & APPROVAL RULES
# ============================================================================
# The proposal agent is allowed to auto-generate within these guardrails.
# Anything outside these bounds triggers escalation to the owner.
# ============================================================================

pricing_rules:
  minimum_deal_size: 0          # TODO: USD; proposals below this get escalated
  maximum_auto_approved: 0      # TODO: USD; proposals above this require owner approval
  discount_ceiling_pct: 0       # TODO: max % discount agent may offer without asking
  payment_terms_default: "Net 15"     # Net 7 / Net 15 / Net 30
  retainer_commitment_months: 3 # Minimum retainer term before rollover

# ============================================================================
# ICP — Ideal Client Profile
# ============================================================================
# The qualification agent scores every lead against this. Leads below the
# threshold are auto-disqualified with a gentle "not the right fit" message.
# ============================================================================

icp:
  # Inferred from services (websites + full-service retainer heavy on GBP + local SEO).
  # Confirm or revise when convenient — see open-questions Q3.
  company_type: "Local service businesses — owner-operated, customers come from 'near me' searches"
  company_size_employees:
    min: 2                      # Very small shops still qualify
    max: 150                    # Beyond this, they usually have in-house marketing
  company_revenue_annual:
    min: 500000                 # TODO: confirm — below $500k annual, retainer math usually doesn't work
    max: 15000000               # TODO: confirm — above this, usually has or wants in-house team
  industries_good_fit:
    # Classic GBP-driven verticals where local-pack ranking = revenue
    - "Home services (HVAC, plumbing, electrical, roofing, flooring, windows)"
    - "Landscaping and outdoor services"
    - "Cleaning and janitorial"
    - "Auto repair and detail"
    - "Medical, dental, chiropractic, physical therapy"
    - "Legal (personal injury, family law, estate)"
    - "Restaurants and food service"
    - "Fitness studios and gyms"
    - "Salons, spas, and barbershops"
    - "Pet services (grooming, boarding, training)"
    - "Real estate brokerages and teams"
    - "Retail with local footprint"
  industries_avoid:
    # TODO: confirm — these are reasonable defaults for a local-SEO agency
    - "Pure e-commerce with no local component"
    - "National-only SaaS / B2B tech"
    - "Multi-level marketing / MLM"
    - "Adult / gambling / firearms (brand risk)"
    - "Crypto / NFT projects"
  geographic_focus:
    - "TODO: which regions do you actually serve? (local to your base, state-wide, national?)"
  budget_minimum_monthly: 0     # TODO: smallest monthly retainer you'll take — see Q4 ask
  decision_maker_roles:
    - "Owner"
    - "Founder"
    - "CEO"
    - "President"
    - "VP Marketing"
    - "Director of Marketing"
    - "Marketing Manager (only if reporting to owner directly)"
  disqualifiers:
    - "No marketing budget / looking for 'free work to prove yourself'"
    - "Equity-only compensation"
    - "Performance-only compensation with no base retainer"
    - "Competing directly with an existing client"
    - "Needs service we don't offer (app dev, TV ads, radio, print)"
    - "Requires industries on the avoid list"
    - "Requests unethical work (fake reviews, link schemes, black-hat SEO)"

  scoring:
    # The qualifier assigns points based on fit. Tune these weights.
    weights:
      industry_match: 30
      budget_fit: 30
      company_size_fit: 20
      decision_maker_reached: 15
      geographic_fit: 5
    thresholds:
      auto_qualify: 70          # Score >= this → auto-route to discovery call
      gray_zone: 50             # 50-69 → escalate to owner for judgment call
      auto_disqualify: 49       # < 50 → polite decline, archived with reason

# ============================================================================
# TEAM — who touches the system besides the owner
# ============================================================================

team:
  owner:
    name: "TODO: your name"
    email: "TODO"
    role: "founder"
    can_approve_proposals: true
    can_approve_contracts: true
  members: []
  # Add employees / contractors here as the team grows:
  # - name: "Contractor Name"
  #   email: "..."
  #   role: "designer" | "writer" | "developer" | "strategist"
  #   can_approve_proposals: false
  #   can_approve_contracts: false

# ============================================================================
# AGENT AUTONOMY LEVELS
# ============================================================================
# Controls how much each agent can do without asking. Levels:
#   - review-required: agent drafts, owner reviews + approves before sending
#   - review-optional: agent sends, summary posted to owner for passive review
#   - fully-autonomous: agent sends, no owner touch unless escalation triggered
#
# RECOMMENDATION: start every agent at review-required for the first month.
# Promote to review-optional after 5 successful runs. Promote to fully-
# autonomous after 20 successful runs + zero escalations.
# ============================================================================

agent_autonomy:
  intake_qualification: "review-optional"   # Low risk, high volume
  proposal: "review-required"               # High risk, client-facing, revenue-impacting
  onboarding: "review-optional"             # Standard content, medium risk
  project_coordinator: "fully-autonomous"   # Internal only, low risk
  deliverable_qc: "review-required"         # Quality gate, owner should see fails
  invoicing: "review-required"              # Start here, promote after v0.1 validation
  reporting: "fully-autonomous"             # Data aggregation, low risk
  retention: "review-required"              # Relationship-sensitive, high risk

# ============================================================================
# OPERATIONAL RHYTHM — when automations fire
# ============================================================================

schedules:
  weekly_client_report:
    day: "Monday"
    time: "08:00"
  monthly_invoicing:
    day_of_month: 1
    time: "09:00"
  renewal_check:
    cadence: "weekly"
    day: "Friday"
    days_before_renewal_to_alert: 30
  payment_chase:
    first_reminder_days_overdue: 3
    second_reminder_days_overdue: 10
    escalation_days_overdue: 21

# ============================================================================
# ESCALATION RECIPIENTS — where do agent questions go
# ============================================================================

escalation:
  primary_channel: "TODO"   # options: email | slack | vault-only | sms
  primary_recipient: "TODO" # email address, slack @handle, phone, or vault note path
  severity_override:
    critical: []    # Channels for critical-only (e.g., SMS only for urgent)
    normal: []      # Default channels

# ============================================================================
# BRAND — how agents write and design
# ============================================================================
# Used by proposal, onboarding, and reporting agents for voice consistency.
# Pairs with the installed `brand` + `market-brand` + `building-brand` skills.
# ============================================================================

brand:
  voice_attributes:
    - "TODO: e.g. direct, bold, not-corporate, confident, straight-talking"
  tone_do:
    - "TODO: e.g. use 'you' and 'we', cite specifics, show receipts"
  tone_avoid:
    - "TODO: e.g. no jargon, no hedging, no generic claims"
  signature_phrases:
    - "TODO: phrases you actually say"
  forbidden_phrases:
    - "TODO: things you'd never say (no 'synergy', no 'world-class', etc.)"
  visual_identity:
    primary_color: "TODO: #hex"
    secondary_color: "TODO: #hex"
    accent_color: "TODO: #hex"
    heading_font: "TODO: Google Font name"
    body_font: "TODO: Google Font name"
    logo_path: "TODO: path to logo svg/png"

# ============================================================================
# METRICS & KPIs
# ============================================================================
# The reporting agent tracks these and alerts on thresholds. See monitoring.md.
# ============================================================================

kpi_targets:
  leads_per_week: 0             # TODO: your target
  qualified_rate_pct: 50        # Baseline, tune after first month
  proposal_to_close_rate_pct: 25 # Baseline
  average_deal_size_usd: 0      # TODO
  monthly_recurring_revenue_target: 0 # TODO
  churn_rate_monthly_pct_max: 5
  owner_hours_per_week_target: 20  # We want to reduce this — tune downward

# ============================================================================
# FEATURE FLAGS
# ============================================================================

features:
  auto_send_proposals: false        # Promote after review-required → review-optional
  auto_send_invoices: false         # Promote after v0.1 validation
  auto_chase_payments: false        # Promote after invoice v0.1 validation
  auto_generate_reports: true       # Low risk
  auto_disqualify_leads: true       # Low risk, high volume
  auto_onboarding_email: false      # Promote after first successful manual run

# ============================================================================
# GoHighLevel (GHL) — primary integration platform
# ============================================================================
# Restore Marketing Co uses GHL heavily. It's the source of truth for contacts,
# opportunities, pipelines, calendars, invoices, workflows, and SMS/email.
#
# Credentials live in .env (GHL_ACCESS_TOKEN, GHL_LOCATION_ID). See .env.example
# at the repo root for the template. NEVER commit credentials.
#
# This section maps GHL's internal IDs to semantic names the automation uses.
# You'll need to pull these from your GHL account once and paste them here.
# ============================================================================

ghl:
  # Base config — usually no need to change
  base_url: "https://services.leadconnectorhq.com"
  api_version: "2021-07-28"

  # Sub-account / location
  # ----------------------------------------------------------------------
  # GHL uses "locations" (a.k.a. sub-accounts) to separate data per client.
  # For Restore Marketing Co's internal automation, use the AGENCY's own
  # location ID (not a client sub-account). Each client that Restore serves
  # will eventually get their own location_id managed separately.
  location_id_source: "env"         # "env" (from GHL_LOCATION_ID) or "config"
  location_id: ""                   # Only used if location_id_source = "config"

  # Sales pipeline — where new leads land and move toward won/lost
  # ----------------------------------------------------------------------
  # Pull these via: python3 scripts/adapters/ghl.py --action pipelines
  pipeline_id_sales: "TODO"         # The main sales pipeline ID
  stage_id_new_lead: "TODO"         # First stage — where inbound leads land
  stage_id_qualified: "TODO"        # After auto-qualification
  stage_id_gray_zone: "TODO"        # Leads needing owner review
  stage_id_discovery_booked: "TODO" # After discovery call booked
  stage_id_discovery_done: "TODO"   # After discovery call held
  stage_id_proposal_sent: "TODO"    # After proposal sent
  stage_id_contract_sent: "TODO"    # After contract sent
  stage_id_won: "TODO"              # Closed won (handoff to onboarding)
  stage_id_lost: "TODO"             # Closed lost (pass reason captured)

  # Tag names — used for filtering, routing, and reporting
  # ----------------------------------------------------------------------
  tags:
    qualified: "restore:qualified"
    gray_zone: "restore:gray-zone"
    disqualified: "restore:disqualified"
    hot_lead: "restore:hot-lead"
    competitor_match: "restore:competitor-match"
    industry_match: "restore:industry-fit"
    onboarding: "restore:onboarding"
    active_client: "restore:active-client"
    at_risk: "restore:at-risk"
    churned: "restore:churned"
    renewal_due: "restore:renewal-due"

  # Native GHL workflows to trigger from automation
  # ----------------------------------------------------------------------
  # Create these workflows in GHL, then paste their IDs here. The automation
  # triggers them instead of duplicating the logic in Python.
  workflow_id_discovery_call_booking: "TODO"   # Sends booking link to qualified leads
  workflow_id_polite_decline: "TODO"           # Sends decline email to disqualified
  workflow_id_gray_zone_nudge: "TODO"          # Optional owner alert
  workflow_id_new_client_onboarding: "TODO"    # Kicks off after contract signed
  workflow_id_payment_chase_1: "TODO"          # First payment reminder
  workflow_id_payment_chase_2: "TODO"          # Second payment reminder
  workflow_id_renewal_warm_up: "TODO"          # 30 days before renewal
  workflow_id_churn_winback: "TODO"            # Post-churn win-back

  # Calendars
  # ----------------------------------------------------------------------
  # Pull via: python3 scripts/adapters/ghl.py --action pipelines (then list_calendars)
  calendar_id_discovery_calls: "TODO"
  calendar_id_kickoff_calls: "TODO"
  calendar_id_renewal_calls: "TODO"

  # Custom field IDs (optional — used for structured lead data)
  # ----------------------------------------------------------------------
  custom_fields:
    lead_score: "TODO"             # Stores the automation's ICP score
    lead_routing: "TODO"           # qualified | gray-zone | disqualified
    industry: "TODO"
    budget_hint: "TODO"
    timeline_hint: "TODO"
    services_interested_in: "TODO"
    audit_findings: "TODO"
    primary_pain_point: "TODO"

  # Rate limiting — GHL API limits
  # ----------------------------------------------------------------------
  rate_limit_per_10s: 100           # Per GHL docs; adapter retries on 429
